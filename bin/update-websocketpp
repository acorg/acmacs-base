#! /bin/bash
source $(dirname $0)/__3rd-party-setup
src_dir websocketpp
if [[ -d "$PACKAGE_DIR" ]]; then
    cd "$PACKAGE_DIR"
    git pull
else
    git clone https://github.com/zaphoyd/websocketpp.git "$PACKAGE_DIR"
fi
( cd $AD_DIR/include && ln -s "$PACKAGE_DIR"/websocketpp . )

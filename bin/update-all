#! /bin/bash
if [ -z "$ACMACSD_ROOT" ]; then
    echo ACMACSD_ROOT not set >&2
    exit 1
fi

failed()
{
    echo "FAILED" >&2
    exit 1
}

trap failed ERR

for P in acmacs-base 3rd-party locationdb hidb seqdb ssm-report; do
    $ACMACSD_ROOT/bin/update-$P
done
